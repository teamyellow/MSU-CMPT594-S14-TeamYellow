<%= div_for comment do %>
  <p>
    <strong>
	Posted <%= time_ago_in_words(comment.created_at) %> ago
	by: <%= User.find(comment.author).username %>
    </strong>
    <br/>
    <%= comment.body %>
  </p>
<% end %>
